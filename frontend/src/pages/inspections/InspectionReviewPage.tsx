import { ArrowLeft, Camera, CheckCircle2, XCircle } from 'lucide-react'
import { useMemo, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { SeverityBadge } from '@/components/badges/Badge'
import { ConfirmDialog, Modal } from '@/components/feedback/Modal'
import { Toast } from '@/components/feedback/Toast'
import { Textarea } from '@/components/forms/Fields'
import { Button } from '@/components/ui/Button'
import { Card } from '@/components/ui/Card'
import { byId, clients, equipment, inspections, nonConformities, reviewAnswers, sites, users } from '@/mocks/domain'
export function InspectionReviewPage() { const { id = 'ins-compressor' } = useParams(); const inspection = byId(inspections, id) ?? inspections[0]; const client = byId(clients, inspection.clientId); const site = byId(sites, inspection.siteId); const item = byId(equipment, inspection.equipmentId); const tech = byId(users, inspection.technicianId); const [approve, setApprove] = useState(false); const [reject, setReject] = useState(false); const [photo, setPhoto] = useState<string | null>(null); const [reason, setReason] = useState(''); const [toast, setToast] = useState(false); const grouped = useMemo(() => Object.entries(reviewAnswers.reduce<Record<string, typeof reviewAnswers>>((acc, answer) => { acc[answer.section] = [...(acc[answer.section] ?? []), answer]; return acc }, {})), []); function done(message: string) { setApprove(false); setReject(false); setToast(true); setTimeout(() => setToast(false), 1800); console.info(message) } return <div className="space-y-6"><div className="flex flex-col gap-4 xl:flex-row xl:items-center xl:justify-between"><div><Link className="mb-3 inline-flex items-center gap-2 text-sm font-semibold text-primary" to="/app/inspections"><ArrowLeft size={16} />Inspecoes</Link><h1 className="text-2xl font-semibold">Revisao da inspecao</h1><p className="text-sm text-muted">{inspection.title} - {item?.name}</p></div><div className="flex gap-2"><Button onClick={() => setApprove(true)}><CheckCircle2 size={17} />Aprovar</Button><Button variant="danger" onClick={() => setReject(true)}><XCircle size={17} />Reprovar</Button></div></div><section className="grid gap-4 md:grid-cols-5"><Summary label="Tecnico" value={tech?.name} /><Summary label="Cliente" value={client?.name} /><Summary label="Local" value={site?.name} /><Summary label="Duracao" value="50 min" /><Summary label="Resultado" value="8 conformes / 4 nao conformes" /></section><div className="grid gap-6 xl:grid-cols-[minmax(0,2fr)_360px]"><div className="space-y-4">{grouped.map(([section, answers]) => <Card key={section} className="p-5"><div className="mb-4 flex items-center justify-between"><h2 className="text-base font-semibold">{section}</h2><span className="text-sm text-muted">{answers.filter(a => !a.nonConformityId).length}/{answers.length}</span></div><div className="space-y-4">{answers.map((answer, index) => <div key={answer.id} className="rounded-fieldops border border-border bg-slate-50 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-medium">{index + 1}. {answer.question}</p><p className={answer.nonConformityId ? 'mt-2 text-sm font-semibold text-danger' : 'mt-2 text-sm font-semibold text-success-dark'}>{answer.result}</p></div><button className="focus-ring rounded-fieldops border border-border bg-white p-2 text-primary" onClick={() => setPhoto(answer.evidence ?? null)}><Camera size={18} /></button></div><p className="mt-3 text-sm text-muted">Observacao: {answer.observation}</p>{answer.nonConformityId && <div className="mt-3 rounded-fieldops border border-warning/40 bg-amber-50 p-3 text-sm"><p className="font-semibold text-warning-dark">Nao conformidade vinculada</p><p className="text-muted">{byId(nonConformities, answer.nonConformityId)?.title}</p></div>}</div>)}</div></Card>)}</div><Card className="h-fit p-5"><h2 className="text-base font-semibold">Nao conformidades</h2><p className="mb-4 text-sm text-muted">4 registros encontrados nesta inspecao</p><div className="space-y-3">{nonConformities.map(nc => <div key={nc.id} className="rounded-fieldops border border-border p-3"><div className="flex items-start justify-between gap-2"><p className="font-medium">{nc.title}</p><SeverityBadge severity={nc.severity} /></div><p className="mt-1 text-xs text-muted">{nc.item}</p></div>)}</div></Card></div><Modal open={Boolean(photo)} title="Evidencia fotografica" onClose={() => setPhoto(null)}>{photo && <div className="space-y-4"><div className="grid h-80 place-items-center rounded-card border border-border bg-gradient-to-br from-primary-light to-white text-primary-dark"><Camera size={64} /></div><p className="text-sm text-muted">Arquivo: {photo} - Localizacao: -23.5015, -47.4526</p></div>}</Modal><ConfirmDialog open={approve} title="Aprovar inspecao?" description="Esta aprovacao e simulada e altera apenas o estado visual do prototipo." confirmLabel="Aprovar inspecao" onCancel={() => setApprove(false)} onConfirm={() => done('approved')} /><Modal open={reject} title="Reprovar inspecao" onClose={() => setReject(false)} footer={<><Button variant="secondary" onClick={() => setReject(false)}>Cancelar</Button><Button variant="danger" disabled={reason.length < 10} onClick={() => done('rejected')}>Confirmar Reprovacao</Button></>}><div className="space-y-4"><Textarea label="Motivo da reprovacao" id="reject-reason" value={reason} onChange={e => setReason(e.target.value)} /><div className="space-y-2"><p className="text-sm font-medium">Itens para correcao</p>{reviewAnswers.slice(0, 6).map(answer => <label key={answer.id} className="flex items-center gap-2 text-sm"><input type="checkbox" defaultChecked={Boolean(answer.nonConformityId)} />{answer.question}</label>)}</div>{reason.length > 0 && reason.length < 10 && <p className="text-xs text-danger">Informe pelo menos 10 caracteres.</p>}</div></Modal><Toast show={toast} message="Decisao registrada no prototipo" /></div> }
function Summary({ label, value }: { label: string; value?: string }) { return <Card className="p-4"><p className="text-xs font-medium text-muted">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></Card> }
