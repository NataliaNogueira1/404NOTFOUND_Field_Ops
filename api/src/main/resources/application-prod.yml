# Production profile. Every secret MUST come from the environment;
# application.yml requires JWT_SECRET (no default), so startup fails fast if unset.
spring:
  jpa:
    show-sql: false

management:
  endpoint:
    health:
      show-details: never

logging:
  level:
    br.com.fieldops: INFO
    org.hibernate.SQL: WARN
